#!/usr/bin/env node

async function main() {
  const { main: runPremiumCLI } = await import('../dist/cli-runner.js')
  await runPremiumCLI()
}

main().catch((error) => {
  console.error('Failed to start PromptBrain CLI:', error.message || error);
  process.exit(1);
})
